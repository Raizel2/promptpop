# macOS
.DS_Store

# Xcode build output
build/
DerivedData/

# Release artefacts(發 release 時會產生,已上傳 GitHub,不進 repo)
*.app.zip

# Xcode per-user state (not shared across machines)
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Packages/

# Local tooling (Claude Code, etc.)
.claude/
